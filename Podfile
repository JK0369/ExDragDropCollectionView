# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ExDragDropCollectionView' do
  use_frameworks!
  
  pod 'Then'
  pod 'SnapKit'
  pod 'Reusable'
end
